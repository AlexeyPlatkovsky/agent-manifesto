# Ledger Service

Rust service that reconciles payment ledgers. Authoritative docs live in `docs/`.

- Never edit a shipped migration in `migrations/`; add a new one.
- Ask before anything that touches production data.
